/**
 * @nestjs/config 12 — только ESM, а Jest грузит модули как CommonJS. Тестам (юнит и интеграционным)
 * пакет не нужен: сервисы получают подставной объект с `get`, здесь только имя класса.
 */
export class ConfigService {}
